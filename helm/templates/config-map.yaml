apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mp-api-gateway.fullname" . }}
  labels:
    {{ include "mp-api-gateway.labels" . | nindent 4 }}
data:
  SERVER_PORT: {{ required "A valid .Values.global.mpApiGatewayPort entry required!" .Values.global.mpApiGatewayPort | quote }}
